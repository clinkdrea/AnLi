# 案理（AnLi）· 律师案件协作平台 需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.3 |
| 编写日期 | 2026-09-12 |
| 来源 | 产品负责人用户访谈（三轮结构化访谈整理） |
| 状态 | 待产品负责人确认 |
| 目标读者 | 产品负责人本人、Trae（AI 编程 IDE）开发协作 |
| v0.2 → v0.3 变更 | 历史案件补录策略调整：不设上线前补录门槛，改为平台开发完成后自行录入；新增「历史案件批量导入」功能（Excel/CSV 模板） |
| v0.1 → v0.2 变更 | ① 移除「搜索他人已结案案件」开关，改为搜索提示；② 新增**利益冲突检索**模块（含判定规则调研）；③ 新增**本地知识库**模块；④ 当事人信息升级为结构化必填字段；⑤ 新增外网访问方案建议；⑥ 产品命名「案理（AnLi）」；⑦ 新增「交付 Trae 开发说明」章节 |

---

## 1. 项目概述

### 1.1 定位与命名

**案理（AnLi）**——为**一家律所内部**打造的案件协作工作台：只服务律师与助理，**不对客户开放**。取"按理"之谐音，寓意"有条理地打理每一个案件"。

关键词：**内部工具、本地优先、离线可用、案件即文件夹、冲突检索、知识沉淀、AI 增强**。

### 1.2 要解决的痛点

| 现状痛点 | 本平台的解法 |
|---|---|
| 案件资料散落在个人电脑、微信、网盘里，找不到 | 每个案件一个文件夹，文件系统即组织方式，全文检索 |
| 期限靠人脑记（上诉期、举证期限、保全续封），漏了就是事故 | 工作流阶段模板 + 期限提醒墙 + 站内红点 |
| 案件进展只有主办律师心里有数 | 案件详情页 + 大事记时间轴，成员随时可见 |
| 讨论散落在微信里，翻不到、对不上案件 | 案件内评论区（@同事），新评论聚合到概览看板 |
| **接新案时利益冲突靠人脑回忆，漏查是执业风险** | **接案立项内置冲突检索：跨全所检索对方当事人、关联主体、同案由、同案号历史记录，自动判定风险等级并生成报告** |
| 律所的经验、法条、判例散落各处，新人无从学起 | 本地知识库：全所共享 + 个人私有，全文检索，AI 可引用 |
| 资料保密要求高，不敢上公有云 | 全部数据存本机 Mac / 局域网 NAS，零云依赖 |
| 卷宗翻不动、写文书费时 | AI 对话助手（以当前案件文件夹为上下文），默认本地 ollama 离线运行 |

### 1.3 范围

**在范围内（首版全量交付）：**

- 用户与角色权限（主办律师 / 协办律师 / 律师助理与实习律师 / 管理员）
- 案件管理（民事、刑事、行政、仲裁、执行、非诉专项、常年法律顾问、其他）
- 当事人结构化信息（我方/对方当事人、关联主体、案由、案号、标的、委托起止时间）
- 工作流与阶段模板（内置标准模板 + 用户可自定义）
- **利益冲突检索与报告（接案立项必经环节）**
- 待办事项与期限提醒、站内红点通知
- 案情记录、案件大事记时间轴
- 资料管理（每案件一文件夹；PDF / Word / Excel / PPT / 图片等；操作留痕）
- OCR 图片文字识别（macOS Vision 框架，离线免费）
- 全文搜索（仅自己参与的案件，搜索时提供冲突检索提示）
- 案件内评论区（@同事）+ 概览看板聚合
- 证据清单
- 文书模板库
- **本地知识库（全所共享 + 个人私有）**
- AI 对话助手（案件文件夹为上下文；本地 ollama / 外部 API 双模式；可引用知识库）
- 操作留痕（审计日志）

**明确不在范围内（见 §11 开放问题）：**

- 对外服务客户（咨询、签约、小程序等）
- 收费 / 开票 / 财务模块
- 手机 App / 小程序 / 邮件通知（预留接口，不做；手机端路径见 §5.5 与开放问题）
- 公网直接访问（首版仅局域网；外网方案已调研，见 §5.5）

### 1.4 用户与规模

- 使用人数：**≤ 20 人**，同一局域网内通过浏览器访问。
- 部署形态：应用运行在**一台 Mac（服务器角色）**上，同事在**同一局域网**用浏览器打开；数据全部存本机，不接受任何云端存储。
- 未来演进：成熟后可迁移到律所 NAS / 局域网服务器；外网访问方案见 §5.5。

---

## 2. 用户与角色

### 2.1 角色定义

| 角色 | 说明 | 典型能力 |
|---|---|---|
| **管理员** | 系统维护者。默认由律所主任或指定人员担任（可兼任主办律师）。 | 用户与角色管理、案件类型与阶段模板维护、知识库分类管理、冲突判定规则维护、系统设置（AI、备份）、查看审计日志；默认可见全部案件的**元数据**（用于冲突检索与审计），案件内容按需加入案件后可见。 |
| **主办律师** | 案件第一责任人。 | 创建案件（含冲突检索）、添加/移除成员、分配任务、审核文书、结案/归档；对自己主办或参与的案件拥有全部操作。 |
| **协办律师** | 参与办案的律师。 | 在被拉入的案件内编辑案情、上传/整理资料、创建任务、评论；不能创建案件、不能结案、不能管理成员。 |
| **律师助理 / 实习律师** | 支持性工作：文书起草、资料整理、跑腿事项。 | 被拉入的案件内可读可写、上传资料、完成任务、评论；不能删除案件、不能结案、不能管理成员。 |

### 2.2 权限模型：角色 × 案件成员（双维度）

1. **角色权限**决定"能做什么操作"（全局能力）。
2. **案件成员制**决定"能看见哪些数据"——**未加入案件 = 完全不可见**（列表、搜索、看板中均不出现该案）。
3. 管理员例外：默认可见全部案件**元数据**（案件名、当事人、案由、案号、承办人、状态、结案时间——供冲突检索与审计），案件详情内容仍需加入案件。

### 2.3 能力矩阵（摘要）

| 操作 | 管理员 | 主办 | 协办 | 助理/实习 |
|---|---|---|---|---|
| 创建案件 | ✅ | ✅ | ❌ | ❌ |
| 发起利益冲突检索 | ✅ | ✅ | ❌ | ❌ |
| 添加/移除案件成员 | 全部案件 | 自己案件 | ❌ | ❌ |
| 编辑案情 / 上传资料 / 创建任务 / 评论 | ✅ | ✅（参与案件） | ✅（参与案件） | ✅（参与案件） |
| 删除资料 / 删除任务 | ✅ | ✅ | ✅（留痕） | ✅（留痕） |
| 结案 / 归档 / 恢复 | ✅ | ✅（自己主办） | ❌ | ❌ |
| 修改案件类型与阶段 | ✅ | ✅（自己主办） | ❌ | ❌ |
| 知识库：创建/编辑共享文档 | ✅ | ✅ | ✅ | ✅（留痕） |
| 知识库：管理分类 | ✅ | ❌ | ❌ | ❌ |
| 用户与角色管理 | ✅ | ❌ | ❌ | ❌ |
| 查看审计日志 | ✅ | ❌ | ❌ | ❌ |
| 搜索范围 | 全所元数据 | 自己参与案件 | 同左 | 同左 |

---

## 3. 核心概念与信息架构

- **案件（Case）**：核心实体。属性：案件名称、编号（自动生成）、案件类型、承办团队、当前阶段、状态（办理中/已结案/已归档）、**当事人结构化信息（见下）**。
- **当事人结构化信息（P0 必填）**：我方当事人（名称、类型：自然人/法人、联系方式）；对方当事人（名称、类型）；**关联主体**（对方当事人/我方当事人的近亲属、关联企业、法定代表人等，可多条）；案由；法院案号（原审/二审/再审/执行案号，未立案可后补）；争议标的额；委托开始日；结案日。→ 这些字段是**冲突检索的数据基础**，创建案件时必填（案号、结案日等可在办案过程中补录）。
- **案件文件夹（Case Folder）**：每个案件在文件系统中对应一个独立文件夹（`案件编号-案件名称`），平台内展示为资料树；文件系统即真相，将来迁 NAS 直接拷贝目录。
- **阶段（Stage）**：案件工作流节点，来自案件类型对应的阶段模板，可自定义增删改。
- **任务（Task）**：待办事项，可挂靠案件 + 阶段，有负责人、截止时间、优先级。
- **提醒（Deadline）**：与案件/阶段关联的关键期限（上诉期、举证期限、保全续封、开庭时间等），到期前提醒、逾期标红升级。
- **案情记录（Case Notes）**：案件内富文本/结构化记录，按时间形成大事记时间轴。
- **评论（Comment）**：案件内留言讨论，支持 @同事；新评论聚合到概览看板。
- **证据（Evidence）**：证据清单条目，可关联资料文件。
- **模板（Template）**：文书模板库，可复用、按案件生成新文档。
- **利益冲突检索（Conflict Check）**：接案立项时跨全所检索当事人与关联主体，按内置判定规则输出风险等级与《利益冲突检索报告》。
- **知识库（Knowledge Base）**：律所知识沉淀——法律法规、司法解释、指导案例、本所办案手记、常见问答；全所共享或个人私有；AI 可引用。
- **AI 助手（Assistant）**：案件内对话式助手，上下文 = 当前案件文件夹（+ 可选知识库）。

---

## 4. 功能需求

> 优先级约定：P0 = 核心闭环，缺了不能用；P1 = 首版必须；P2 = 首版含基础能力，细节后续打磨。所有功能均为全量交付范围。

### 4.1 概览看板（P0）

登录后的首页，一屏看清"今天该干什么、有什么新动静"。

- **我的待办**：跨案件聚合，按截止时间排序；逾期标红。
- **即将到期提醒**：3 天内到期的期限/任务汇总。
- **最新评论动态**：跨案件聚合，不需要打开具体案件就能看到新评论内容与来源案件；点击跳转到对应案件评论区。
- **我的案件**：我参与的案件入口（按最近活跃排序）。
- **未读红点汇总**：顶部导航显示未读评论 / 提醒数量。

**验收要点**：任意案件的评论产生后，看板在刷新后立即出现该动态；红点数字与实际未读一致。

### 4.2 案件管理（P0）

- 创建案件流程：选择案件类型（民事/刑事/行政/仲裁/执行/非诉专项/常年法律顾问/其他）→ 填写**当事人结构化信息（必填）** → **发起利益冲突检索并确认报告**（见 4.8）→ 自动套用该类型阶段模板 → 自动创建案件文件夹。
- 案件列表：筛选（类型、阶段、状态、我的角色）、搜索、最近访问。
- 案件详情页 Tab：**概览**（关键信息 + 提醒 + 最新动态）、**案情记录**、**资料**、**证据清单**、**时间轴**、**评论**、**AI 助手**、**冲突报告**（如有）。
- 结案 / 归档 / 恢复：主办律师操作；结案时需填写结案日期与结案方式（自动用于冲突检索的一年期判定）。
- 自动编号：如 `2026-MS-001`（年份-类型-序号）。
- **历史案件批量导入（P1）**：案件列表提供「批量导入」入口（仅管理员），下载 Excel 模板 → 填写历史案件元数据（案件名称、类型、我方/对方当事人、案由、案号、承办人、委托开始日、结案日）→ 上传导入 → 逐行校验并报告结果；导入成功即参与冲突检索。为开发完成后律所自行补录历史案件而设计。

### 4.3 工作流与阶段模板（P0）

- 每种案件类型内置**标准阶段模板**（明细见附录 A），创建案件时自动套用。
- **允许用户自己改**：案件创建后，主办律师/管理员可增删阶段、调整顺序、重命名，不影响模板本身。
- 模板管理：管理员可在设置中维护各案件类型的默认模板（改模板只影响新建案件）。
- 阶段流转：标记阶段开始/完成时间；阶段可挂任务与期限。
- 同一案件可多线并行（如"一审"与"财产保全"并行推进）。

**验收要点**：新建民事案件自动生成"接案立项（含冲突检索）→诉前准备→立案受理→庭前→开庭→判决→判后"阶段；主办律师可删掉任一阶段。

### 4.4 待办与期限提醒（P0）

- 任务：标题、描述、负责人、截止时间、优先级（高/中/低）、状态（待办/进行中/完成）、关联案件与阶段。
- 期限提醒类型（内置常用）：上诉期（判决送达后 15 日）、举证期限、诉讼时效、保全到期/续封、开庭时间、审限、执行异议期、刑期节点等；支持自定义期限。
- 提醒规则：到期前 7 / 3 / 1 天站内提醒（可配置），到期日与逾期标红，**逾期自动升级通知主办律师**。
- 通知渠道：首版仅**站内红点 + 看板聚合**；预留邮件 / 小程序 / App 推送接口（P2）。

### 4.5 案情记录与大事记时间轴（P0）

- 案情记录：富文本（加粗、列表、标题），每条记录含作者、时间，可关联阶段。
- 时间轴：案件详情"时间轴"Tab 按时间正序展示案情记录 + 阶段流转 + 关键任务完成事件，形成案件大事记。

### 4.6 资料管理（每案件一个文件夹）（P0）

- 案件详情"资料"Tab 展示案件文件夹的文件树；支持**上传、新建子文件夹、重命名、下载、预览、删除**。
- 支持文件类型：PDF、Word、Excel、PPT、图片（jpg/png/heic 等）及任意其他文件（原样存储）。
- 图片 OCR：上传图片与扫描版 PDF 后，后台用 **macOS Vision 框架离线识别文字**（免费、不联网），提取文字进入全文索引，供搜索与 AI 使用；识别结果附在文件侧栏可见。手写体识别率有限，界面标注提示。
- **操作留痕**：谁在何时上传/下载/预览/删除/修改了哪个文件，全量记入审计日志（见 4.14）。
- 上传限制：单文件大小默认 ≤ 500MB（可配置）。

### 4.7 全文搜索（P0）

- 范围：案件名称、案情记录、评论、任务、文件名、文件内容（已提取文字的文档与 OCR 结果）。
- **仅搜索自己参与的案件**（含自己参与且已结案的案件），**不提供跨案开关**。
- **搜索提示（替代开关）**：搜索页固定提示条——「搜索范围仅限您参与的案件。接案前如需核查对方当事人是否与本所历史案件存在利益冲突，请使用【利益冲突检索】功能（覆盖全所案件元数据）。」
- 技术：SQLite FTS5（中文用 trigram 分词，无需外部服务），毫秒级响应。
- 结果页：命中关键词高亮，按类型分组（案件/笔记/评论/文件），点击直达。

**验收要点**：协办律师搜索不到未参与案件的任何内容；搜索页提示条始终可见，可一键跳转到冲突检索。

### 4.8 利益冲突检索（P0，新增核心模块）

**背景与依据**：律师接案前须进行利益冲突审查。《律师法》第 39 条禁止在同一案件中为双方代理、禁止代理与本人或近亲属有利害冲突的法律事务；《律师执业行为规范》第 50、51 条列明应当回避与须告知同意的情形（完整默认判定规则见附录 C）。

**典型场景（产品负责人提供）**：一年前作为 A 的代理人已与 B 完成案件，一年后 B 上门委托代理 B——系统需检索出 B 曾作为对方当事人出现在本所历史案件中，并依据结案时间、案由、案号判断属于哪一档风险，生成报告供律师决定。

#### 检索所需数据（即系统必须结构化存储的字段）

| 数据 | 用途 |
|---|---|
| 对方当事人名称 + 类型 | 精确/模糊匹配全所历史案件的「我方/对方当事人」 |
| 关联主体（近亲属、关联企业、法定代表人） | 查出"张三本人没冲突，但张三名下公司有冲突"的情形 |
| 案由 / 法律事务性质 / 争议标的 | 判断新旧事务是否为"同一法律事务" |
| 法院案号（原审/二审/再审/执行） | 判断是否为"同一案件的后续审理或处理"（绝对禁止情形） |
| 结案时间（委托关系终止时间） | 判定是否在"终止后一年内"（须告知+书面同意情形） |
| 承办律师 / 同所其他律师在办案件 | 判断同所律师是否正在代理利害冲突方 |
| 案件状态（办理中/已结案/已归档） | 过滤在办案件（冲突更严格） |

#### 功能流程

1. 入口：创建案件流程第二步；也可从案件详情"冲突报告"Tab 对已有案件补做。
2. 输入：自动带入案件已填的当事人信息，可补充关联主体、案由关键词。
3. 检索：在**全所案件元数据**范围内匹配（不含案情内容，保证保密），命中结果列出——当事人出现在哪些案件、身份（我方/对方/第三人）、案号、案由、承办人、状态、结案时间。
4. **风险判定**（内置规则，可配置）：每条命中自动归类——
   - 🚫 **禁止代理**：同一案件后续程序（二审/再审/执行/仲裁关联）接受对方当事人委托等（规范第 50 条）；
   - ⚠️ **须告知并取得书面同意**：委托关系终止后一年内就同一法律事务接受对方当事人委托等（规范第 51 条）；
   - 👁 **审慎评估**：一年以上但可能利用原委托人保密信息；对方当事人关联主体在本所其他案件出现等；
   - ✅ **未发现冲突**。
5. **AI 辅助判定（本地 ollama，可离线）**：
   - 主体归一：识别"张三"与"张三的公司/张某某"是否为同一主体（需人工确认）；
   - 纠纷相似度：新旧案件案由、标的、事实关键词比对，辅助"同一法律事务"判断；
   - 规则匹配：将命中结果与内置规则条款对照，输出引用说明（如"依据《律师执业行为规范》第 51 条第 5 项……"）。
6. 生成《利益冲突检索报告》：含检索条件、命中清单、风险结论、规则引用、AI 辅助意见；报告**存入案件文件夹**并留痕。
7. 处理结论（主办律师必选，留痕）：`已确认无冲突，同意接案` / `已告知委托人并取得书面同意` / `放弃接受委托`。存在 🚫 情形时，系统弹强提示要求填写处理说明后才可继续立项。

**验收要点（产品负责人示例）**：一年前我代理 A 诉 B（已结案），现 B 委托我代理其对 A 的新诉讼——系统应检索出 B 作为对方当事人出现在该历史案件中，依据结案时间与案由判断给出 ⚠️ 或 👁 提示并引用对应条文，生成报告存档。

**历史案件补录（上线后）**：冲突检索对新案件即时生效；对历史案件的覆盖取决于录入数据。律所目前暂无大量历史案件数据，待平台开发完成后，通过「历史案件批量导入」（见 4.2）自行补录即可，无需在上线前完成。

### 4.9 评论区（P0）

- 每个案件内一个评论区；支持 @同事（被 @ 者收红点通知）。
- 支持引用回复（楼层式）；可编辑/删除自己的评论（留痕）。
- 新评论**实时出现在概览看板**的动态区。

### 4.10 证据清单（P1）

- 表格化条目：证据名称、证据来源、证明目的、分组（可自定义分组）、关联文件（从案件资料中选取）、备注。
- 支持导出为 Excel/Word 清单（P2 可后置，首版支持复制为表格文本）。

### 4.11 文书模板库（P1）

- 管理员/主办律师可维护模板（新建、编辑、分类），全所共享。
- 从模板新建文书：选择模板 + 关联案件 → 生成文档并自动存入该案件文件夹。
- 模板内容支持占位符（如 `{{当事人}}`、`{{案件编号}}`），生成时自动填充。

### 4.12 本地知识库（P1，新增模块）

**定位**：律所自己的知识沉淀库，全部本地存储、离线可用；与 AI 助手联动形成"所内知识 + 案件资料"的问答能力。

- **内容类型**：法律法规、司法解释、指导案例/裁判观点、本所办案手记、常见问题 Q&A、文书范本说明。
- **可见性**：`全所共享`（默认）或 `个人私有`（仅作者可见）。
- **组织方式**：分类（管理员维护，如：民事/刑事/行政/劳动/公司…）+ 标签 + 全文检索。
- **录入方式**：在线编辑（Markdown）；离线导入（Word/PDF/Markdown 文件批量导入，图片走 OCR）。
- **检索**：FTS5 全文检索（标题 + 内容 + 标签）；向量语义检索（本地 ollama embedding + sqlite-vec）列为 P2 增强。
- **与 AI 联动**：案件内 AI 助手可开启「引用知识库」开关，回答时同时基于当前案件文件夹 + 知识库，并标注引用来源。

**验收要点**：导入一份 PDF 判例后能用正文关键词搜到；个人私有文档对其他账号不可见；AI 回答能引用知识库条目并显示来源标题。

### 4.13 AI 对话助手（P1）

- 入口：案件详情页"AI 助手"Tab。
- **上下文默认 = 当前案件文件夹**：AI 能读取该案件文件夹内文件的内容（含 OCR 文字）与案情记录来回答，**只能读取当前案件**，不能读取其他案件。
- 可选开启「引用知识库」（见 4.12），回答时标注引用来源。
- 会话历史**按案件持久保存**，下次打开可继续。
- 双模式：
  1. **本地 ollama**（默认，推荐）：完全离线，数据不出本机。
  2. **外部 API**（OpenAI 兼容接口）：需在设置中配置 baseURL / key / 模型；**首次启用时弹窗明确告知"案件资料将被发送至第三方服务器"，需用户手动确认**。
- 典型用例：卷宗材料总结、按证据找关键事实（如"这个案子的借条金额是多少"）、文书起草辅助、时间轴整理、**冲突检索辅助判定**（见 4.8）。
- 合规提示（写入界面）：涉及身份证号、手机号、银行账号等敏感信息时，建议优先使用本地模型。
- 设置项：默认模型、温度、外部 API 配置、本地模型选择。

**验收要点**：AI 能引用资料文件内容回答；切换案件后拿不到前一案件的资料；未开启外部 API 时 AI 功能在断网状态下可用（本地模型）。

### 4.14 操作留痕（审计日志）（P0）

- 记录事件：登录/登出、案件创建/修改/结案、成员变更、权限变更、**冲突检索与处理结论**、文件上传/下载/预览/删除、任务与评论操作、知识库编辑、AI 会话使用记录（含调用了哪些文件）。
- 每条日志：时间、操作人、对象（案件/文件路径）、动作、结果。
- 管理员在系统设置中查看与导出（CSV）；默认保留 1 年（可配置）。

### 4.15 通知中心（P1）

- 站内红点：待办到期、被 @、案件有新评论、逾期升级。
- 通知列表页：按时间倒序，可标记已读。
- 预留通知渠道接口（邮件 / 小程序 / App 推送）——首版不实现。

### 4.16 系统设置（P1）

- **用户与角色**：账号增删改、重置密码、角色分配（仅管理员）。
- **案件类型与阶段模板**：维护默认模板（仅管理员）。
- **冲突判定规则**：查看与增改内置判定规则（仅管理员；默认内置全国律协规则，见附录 C）。
- **知识库分类**：维护分类（仅管理员）。
- **AI 配置**：ollama 地址 / 外部 API 配置与启用确认。
- **审计日志**：查看与导出。
- **备份**：一键备份（SQLite + 案件文件夹打包为 zip 到指定位置）、提示配置定期备份。
- **历史案件导入**：下载模板、批量导入、查看校验结果（仅管理员）。

---

## 5. 非功能需求

### 5.1 部署与运行环境

- 服务器：一台 macOS（Apple Silicon 或 Intel），安装 Node.js 运行时。
- 客户端：同事在同一局域网内用现代浏览器（Chrome / Edge / Safari 最新版）访问 `http://<Mac的局域网IP>:<端口>`。
- **数据完全本地**：SQLite 数据库 + 案件文件夹均存于 Mac 本机，**无任何云端依赖**；仅当用户主动启用外部 AI API 时才有外部网络请求（需显式确认）。
- 常驻运行：随 Mac 开机自启（launchd / pm2），崩溃自动重启。
- NAS 迁移预留：整个应用数据目录拷贝即可迁移；NAS 服务器形态（Docker / Linux）列为可选演进。

### 5.2 性能

- 规模：≤ 20 名用户、数百案件、单案件文件夹 GB 级资料，常规操作响应 < 1s，搜索 < 2s，冲突检索 < 3s。
- 大文件上传走流式处理，不整文件读入内存；OCR 后台队列处理，不阻塞界面。

### 5.3 安全与保密（重要）

- 登录鉴权：账号 + 密码（bcrypt 存储），会话超时；失败锁定策略（连续 5 次锁定 15 分钟）。
- 文件访问全部经后端鉴权，**不直接暴露文件目录**。
- 局域网内 HTTP 传输提示：首版局域网内 HTTP 可用；如未来开放外网访问，必须升级 HTTPS（见 5.5）。
- 操作留痕（4.14）全面覆盖；冲突检索只返回元数据，不泄露他案案情。
- 数据备份：SQLite + 案件文件夹需可一键备份；建议配置每日自动备份到 NAS/移动硬盘（开放问题 #3）。
- 合规提示：案件资料属当事人保密信息（《律师法》第 38 条保密义务），本地存储是当前最稳妥方案；启用外部 AI 前务必确认脱敏。

### 5.4 可用性与兼容

- 界面语言：中文；产品名「案理」。
- 浏览器兼容：Chrome / Edge / Safari 近两年版本。
- 移动端不做（路径见 5.5 与开放问题 #6），但页面在窄屏下不崩溃（可水平滚动）。

### 5.5 外网访问方案（调研建议，暂不实施）

> 首版仅局域网使用即可。以下为将来"出差/在家/手机"访问时可选路径，调研对比与建议如下：

| 方案 | 说明 | 成本 | 优点 | 缺点 |
|---|---|---|---|---|
| **① VPN 组网：ZeroTier**（建议首选） | 每台设备装客户端，加入虚拟局域网，浏览器照常访问 | 免费（约 25 节点，覆盖 20 人团队） | 零成本、设备级加密、对应用透明、最快落地 | 每台设备需装客户端 |
| ①′ Tailscale | 同上，体验更好 | 免费版仅 3 用户，20 人需付费或自建 Headscale | 体验最佳 | 20 人免费额度不足 |
| ② 内网穿透（frp 等） | 把 Mac 端口映射到公网域名 | 需一台公网服务器（约 ¥30+/月）或免费节点 | 浏览器直开、无需客户端 | 暴露公网，安全要求高（HTTPS + 强鉴权必需） |
| ③ 云服务器反向代理 + WireGuard | 轻量云服务器做隧道 + 域名 + HTTPS | 约 ¥30–60/月 | 体验最好（https 域名）、可控 | 有月费、需配置 |
| ④ 迁移到 NAS/自建服务器 | 服务迁到 NAS 或常开小主机 | 硬件成本 | 24h 在线、与备份一体化 | 需运维 |

**建议**：需要外网访问时，先上 **ZeroTier**（零成本、最安全、最快）；对体验有更高要求再评估方案 ③。**手机端**将来可先用「ZeroTier + 浏览器访问（响应式/PWA）」低成本跑起来，原生 App 后置。无论哪种方案，开放外网前必须：HTTPS + 强口令 + 会话加固。

---

## 6. 技术方案建议

> 选型原则：适合「产品负责人 + Trae（AI 编程）协作开发」——生态成熟、AI 生成质量高、单仓库可维护、零云依赖。

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | **React + TypeScript + Vite** | 生态最大，AI 协作最顺手；shadcn/ui 风格组件（Tailwind CSS）保证 B 端质感 |
| 后端 | **Node.js + Fastify（TypeScript）** | 与前端同语言；Fastify 轻量、性能好、类型友好；NestJS 亦可，视规模取舍 |
| 数据库 | **SQLite（better-sqlite3）** | 单文件零运维，20 人规模绰绰有余；FTS5（trigram 分词）做中文全文搜索 |
| 文件存储 | **文件系统**（每案件一文件夹） | 文件系统即真相，NAS 迁移 = 拷贝目录 |
| OCR | **macOS Vision 框架**（Swift 小命令行工具） | 系统自带、离线免费；后端以子进程调用 |
| AI | **OpenAI 兼容接口** | 本地 ollama 天然兼容；外部 API 同接口切换 |
| 向量检索（P2） | **ollama embedding + sqlite-vec** | 知识库语义搜索，全部本地离线 |
| 鉴权 | JWT + bcrypt；登录会话 Cookie（HttpOnly） | 标准做法 |

**建议的项目结构（单仓库）：**

```
anli/
├── client/          # React + Vite 前端
├── server/          # Fastify 后端
│   ├── routes/      # API 路由
│   ├── services/    # 案件/冲突检索/搜索/OCR/AI/知识库 服务
│   └── db/          # SQLite schema 与迁移
├── ocr-tool/        # Swift 小工具（Vision OCR）
├── data/            # 运行时数据：SQLite + 案件文件夹（不入库）
└── docs/            # 本需求文档（PRD.md）与设计决策
```

**关键实现提示（供 Trae 参考）：**
- FTS5 中文检索用 `tokenize='trigram'`（SQLite ≥ 3.34）。
- OCR 队列：上传后异步排队，识别结果写入数据库索引，文件本身保持原样。
- 冲突检索只查元数据表（不查案件内容全文），保证保密边界。
- 外部 AI 请求只在用户开启外部模式时发生；默认走 `http://localhost:11434`（ollama）。

---

## 7. 数据模型草案

| 实体 | 关键字段 |
|---|---|
| User | id、姓名、账号、密码哈希、角色（admin/lead/co/assistant）、状态 |
| Case | id、编号、名称、类型、主办人、当前阶段、状态（办理中/已结案/归档）、文件夹路径、创建时间 |
| **CaseParty** | id、case_id、类型（我方/对方/第三人）、主体名称、主体类型（自然人/法人）、联系方式、关联关系 |
| **RelatedParty** | id、case_id、主体名称、关系类型（近亲属/关联企业/法定代表人/其他） |
| **CaseLegalInfo** | id、case_id、案由、法院案号、标的额、委托开始日、结案日、结案方式 |
| CaseMember | case_id、user_id、加入时间 |
| StageTemplate | id、案件类型、阶段列表（JSON，有序） |
| CaseStage | id、case_id、名称、顺序、开始/完成时间、自定义标志 |
| Task | id、case_id、stage_id?、标题、描述、负责人、截止时间、优先级、状态 |
| Deadline | id、case_id、类型（上诉期/举证期/自定义）、到期时间、提醒规则 |
| CaseNote | id、case_id、stage_id?、内容（富文本）、作者、时间 |
| Comment | id、case_id、parent_id?、内容、@提及用户（JSON）、作者、时间 |
| Evidence | id、case_id、分组、名称、来源、证明目的、关联文件id、备注 |
| Template | id、名称、分类、内容、占位符信息、创建人 |
| FileRecord | id、case_id、相对路径、文件名、类型、大小、OCR状态、OCR文本索引 |
| **ConflictReport** | id、case_id、检索条件（JSON）、命中记录（JSON）、风险判定、规则引用、AI 意见、处理结论、操作人、时间 |
| **ConflictRule** | id、等级（禁止/告知同意/审慎）、条款要点、引用条文、启用状态 |
| **KnowledgeCategory** | id、名称、排序 |
| **KnowledgeDoc** | id、标题、分类、标签、内容/文件路径、可见性（全所/私有）、创建人、更新时间 |
| AuditLog | id、时间、user_id、动作、对象类型、对象id、详情 |
| AIConversation | id、case_id、模型、消息列表（JSON）、创建/更新时间 |
| Notification | id、user_id、类型（待办/评论/提醒/升级）、关联对象、已读状态 |

---

## 8. 页面 / 路由清单

| 路由 | 页面 | 说明 |
|---|---|---|
| `/login` | 登录页 | 账号密码登录 |
| `/` | 概览看板 | 待办、提醒、评论动态、我的案件、红点 |
| `/cases` | 案件列表 | 筛选 + 搜索 + 新建案件 |
| `/cases/new` | 新建案件向导 | 类型 → 当事人信息 → **冲突检索** → 立项 |
| `/cases/import` | 历史案件批量导入 | 模板下载 → 上传 → 校验报告（仅管理员） |
| `/cases/:id` | 案件详情 | Tab：概览 / 案情记录 / 资料 / 证据清单 / 时间轴 / 评论 / AI 助手 / 冲突报告 |
| `/conflicts` | 利益冲突检索 | 输入条件 → 检索 → 报告 |
| `/search` | 全文搜索 | 仅自己参与的案件；提示条引导冲突检索 |
| `/knowledge` | 知识库 | 分类浏览 + 搜索 + 新建/导入文档 |
| `/templates` | 文书模板库 | 模板列表、编辑、新建文书 |
| `/notifications` | 通知中心 | 未读/已读通知 |
| `/settings/profile` | 个人设置 | 改密码 |
| `/settings/admin` | 系统设置（管理员） | 用户与角色、阶段模板、冲突规则、知识库分类、AI 配置、审计日志、备份 |

---

## 9. 实施路线图（开发顺序建议）

> 需求为全量交付（一步到位）。以下顺序仅作为**开发实施顺序**建议，避免返工：

| 里程碑 | 内容 | 产出 |
|---|---|---|
| M0 骨架 | 项目初始化、登录鉴权、权限模型、SQLite schema、案件 CRUD + 文件夹 + 当事人结构化字段 | 可登录、可建案件 |
| M1 案件核心 | 资料管理（上传/预览/下载 + 留痕）、案情记录、时间轴、搜索（文件名+文本） | 核心办案闭环 |
| M2 协作与工作流 | 阶段模板与自定义、任务与期限提醒、评论区 + @、概览看板、**冲突检索模块**、**历史案件批量导入**、证据清单、模板库 | 团队协作闭环 |
| M3 增强 | OCR 队列、FTS5 全文、审计日志报表、**本地知识库**、AI 助手（ollama/外部 + 冲突辅助 + 知识库引用）、备份脚本 | 全量功能 |
| M4 打磨 | 红点通知中心、导出、性能与边界测试、部署文档（开机自启、局域网访问说明） | 交付 |

---

## 10. 验收标准（汇总）

1. 三种角色 + 管理员登录后，看到的案件列表严格符合权限（未加入案件不可见、搜索不到）。
2. 新建民事案件自动生成标准阶段模板（含接案立项的冲突检索环节）；主办律师可增删改阶段。
3. 创建案件时当事人信息为必填；案号、结案日可后补。
4. **冲突检索（产品负责人示例）**：一年前代理 A 诉 B 已结案，现 B 委托代理其对 A 的新诉讼——检索出 B 的历史案件记录，依据结案时间与案由给出 ⚠️/👁 判定并引用条文，生成报告存档，主办律师填写处理结论后方可立项。
5. 冲突检索只返回元数据，任何用户无法通过冲突检索看到他人案件的案情内容。
6. 上传一张含文字的卷宗照片后，能用照片里的文字搜索到该文件与案件。
7. 任意案件的新评论出现在看板动态区，无需进入案件即可查看。
8. 逾期任务/期限在主办律师的看板与红点中出现升级提醒。
9. 文件的每次上传/下载/预览/删除均可在审计日志中查到（操作人 + 时间）。
10. 知识库：导入 PDF 判例可用正文关键词搜到；私有文档对其他账号不可见；AI 可引用知识库并标注来源。
11. AI 助手能基于当前案件文件夹内容回答问题；切换案件后无法访问前一案件的资料。
12. 断网状态下（除外部 AI 模式外）全部核心功能可用。
13. 一键备份可打包数据库 + 全部案件文件夹，并可恢复。
14. 按模板批量导入历史案件（Excel/CSV）成功，导入的案件立即参与冲突检索。

---

## 11. 风险与开放问题（待确认项）

| # | 问题 | 当前默认 | 影响 |
|---|---|---|---|
| 1 | 管理员由谁担任？默认可见全所案件元数据、内容需加入案件——是否可接受？ | 主任/指定人；可见元数据 | 权限细节 |
| 2 | 启用外部 AI 的合规确认流程（是否需律所负责人书面同意） | 界面显式确认 + 默认本地模型 | 保密合规 |
| 3 | 自动备份策略（频率、目标位置：NAS/移动硬盘） | 首版提供一键备份 + 手动脚本 | 数据安全 |
| 4 | 外网访问方案最终拍板（建议 ZeroTier，见 §5.5） | 首版不做，仅调研建议 | 架构预留 |
| 5 | 收费/开票记录是否后续补充为模块 | 不在首版 | 范围 |
| 6 | 手机端启动时机 | 取决于开发进度；建议核心稳定后先做响应式/PWA + ZeroTier 访问，原生后置 | 路线图 |
| 7 | 历史案件录入时机与范围 | **已确认**：平台开发完成后由律所自行批量导入；系统提供 Excel 模板导入功能，不设上线前门槛 | 冲突检索覆盖面 |
| 8 | 知识库向量语义检索是否首版做 | 默认后置（P2），首版用全文 + 标签检索 | 范围 |
| 9 | 冲突判定规则是否需要按律所制度增补 | 内置全国规则（附录 C），支持在设置中增改 | 合规 |

---

## 12. 交付 Trae 开发说明

**交接步骤：**
1. 将本文件保存为项目仓库 `docs/PRD.md`。
2. 在 Trae 中创建新项目（技术栈见 §6），把 `docs/PRD.md` 放入仓库。
3. 用以下启动提示词让 Trae 先产出技术设计，确认后再写代码：

> **建议启动提示词（可直接粘贴给 Trae）：**
> 请先完整阅读 docs/PRD.md。这是「案理」律师案件协作平台的完整需求文档。请先输出 M0 阶段的技术设计供我确认：目录结构、SQLite 表结构（SQL）、后端 API 清单（路由 + 请求/响应）、前端路由与页面骨架，暂不写业务代码。必须遵守的硬约束：① 所有数据只存本机文件系统与 SQLite，禁止任何云端存储和外部服务依赖（除用户显式开启的外部 AI API）；② 权限 = 角色 × 案件成员，未加入案件完全不可见；③ 所有写操作记录审计日志；④ OCR 使用 macOS Vision 框架离线实现；⑤ 全文搜索用 SQLite FTS5 trigram；⑥ AI 默认接本地 ollama（OpenAI 兼容接口）；⑦ 界面全中文，产品名「案理」。设计确认后，按第 9 章里程碑顺序开发。

**给 Trae 的硬约束清单（务必在每次会话开头重申）：**
- 本地优先、零云依赖（外部 AI API 是唯一例外，需用户显式开启并确认）。
- 每案件一个文件夹，文件系统即真相。
- 权限模型双维度：角色 × 案件成员。
- 全程操作留痕。
- OCR 用 macOS Vision（离线、免费）。
- FTS5 trigram 中文分词。
- 界面中文；组件风格参考 shadcn/ui。

---

## 附录 A · 内置阶段模板明细

### A1 民事一审（标准模板）
接案立项（**利益冲突检索 → 冲突报告确认** → 收费确认 → 签订委托合同）→ 诉前准备（证据收集、财产保全评估）→ 立案受理 → 庭前程序（举证期限、证据交换、庭前会议）→ 开庭审理 → 裁判结果 → 判后工作（上诉期 15 日 / 申请执行）

### A2 民事二审
上诉 → 二审立案 → 庭前准备 → 开庭/书面审理 → 裁判 → 执行衔接

### A3 民事再审
申请再审 → 审查/听证 → 再审审理 → 裁判

### A4 刑事
侦查阶段（会见、取保候审申请、提交法律意见）→ 审查起诉（阅卷、认罪认罚协商）→ 一审（庭前会议、开庭、辩护）→ 二审/复核 → 申诉

### A5 行政
行政复议 → 行政诉讼一审 → 二审 → 申请执行

### A6 仲裁
申请仲裁 → 组庭 → 证据交换 → 开庭 → 裁决 → 申请执行/申请撤销裁决

### A7 执行
申请执行 → 立案 → 财产查控 → 处置变现 → 款项发放 → 结案（执行完毕/终结本次执行）

### A8 非诉专项
立项（冲突检索）→ 尽职调查 → 方案设计 → 实施推进 → 成果交付 → 结案

### A9 常年法律顾问
签约（冲突检索）→ 日常咨询 → 合同审查 → 培训/专项支持 → 年度续约评估

### A10 其他
自定义（空模板，由用户自行搭建）

> 所有模板均可由管理员在系统设置中修改；案件创建后亦可单独调整，互不影响。诉讼类案件均含「接案立项（冲突检索）」首环节。

---

## 附录 B · 能力矩阵（完整版）

| 操作 | 管理员 | 主办 | 协办 | 助理/实习 |
|---|---|---|---|---|
| 创建案件 | ✅ | ✅ | ❌ | ❌ |
| 发起利益冲突检索 | ✅ | ✅ | ❌ | ❌ |
| 查看冲突报告 | ✅（全部） | ✅（自己案件） | ✅（参与案件） | ✅（参与案件） |
| 确认冲突处理结论 | ✅ | ✅（自己主办） | ❌ | ❌ |
| 添加/移除案件成员 | 全部案件 | 自己案件 | ❌ | ❌ |
| 编辑案情 / 上传资料 / 创建任务 / 评论 | ✅ | ✅（参与案件） | ✅（参与案件） | ✅（参与案件） |
| 删除资料 / 删除任务 | ✅ | ✅ | ✅（留痕） | ✅（留痕） |
| 结案 / 归档 / 恢复 | ✅ | ✅（自己主办） | ❌ | ❌ |
| 修改案件类型与阶段 | ✅ | ✅（自己主办） | ❌ | ❌ |
| 知识库：创建/编辑共享文档 | ✅ | ✅ | ✅ | ✅（留痕） |
| 知识库：个人私有文档 | 仅本人 | 仅本人 | 仅本人 | 仅本人 |
| 知识库：管理分类 | ✅ | ❌ | ❌ | ❌ |
| 维护冲突判定规则 | ✅ | ❌ | ❌ | ❌ |
| 用户与角色管理 | ✅ | ❌ | ❌ | ❌ |
| 查看审计日志 | ✅ | ❌ | ❌ | ❌ |
| 全文搜索范围 | 全所元数据 | 自己参与案件 | 同左 | 同左 |

---

## 附录 C · 利益冲突判定规则默认清单

> 依据《律师法》第 38、39 条与《律师执业行为规范》第 50、51 条整理（条文要点，非逐字引用）。内置为系统默认规则，律所可在系统设置中增改（见开放问题 #9）。最终是否接案由律师按律所制度人工决定，平台提供判定辅助与留痕，不做自动拦截（🚫 情形除外，见 4.8 处理结论）。

### 🚫 禁止代理（无条件回避）

| 情形 | 条文依据（要点） |
|---|---|
| 同一案件中为双方当事人担任代理人，或代理与本人/近亲属有利害冲突的事务 | 《律师法》第 39 条；规范第 50 条第 1 项 |
| 近亲属是对方当事人的法定代表人或代理人 | 规范第 50 条第 2 项 |
| 曾作为行政机关工作人员、审判人员、检察人员、仲裁员办理过该事项/案件，转任律师后又办理该案 | 规范第 50 条第 3 项 |
| 同一律所不同律师同时担任同一刑事案件的被害人代理人与被告人辩护人（县域仅一所且双方同意除外） | 规范第 50 条第 4 项 |
| 民诉/行政诉讼/仲裁中，同所不同律师代理争议双方；或本所及工作人员为一方当事人、本所律师代理对方 | 规范第 50 条第 5 项 |
| 非诉业务中同所律师同时代理彼此有利害关系的各方（各方共同委托除外） | 规范第 50 条第 6 项 |
| **委托关系终止后，同一律所或同一律师在同一案件后续审理或处理中又接受对方当事人委托** | 规范第 50 条第 7 项 |
| 其他按行业经验判断应主动回避的情形 | 规范第 50 条第 8 项 |

### ⚠️ 须告知委托人并取得书面同意

| 情形 | 条文依据（要点） |
|---|---|
| 接受民诉/仲裁一方委托，同所律师是对方当事人近亲属 | 规范第 51 条第 1 项 |
| 担任刑案辩护人，同所律师是被害人近亲属 | 规范第 51 条第 2 项 |
| 同所接受正在代理案件的对方当事人委托的其他法律业务 | 规范第 51 条第 3 项 |
| 律所与委托人存在服务关系，该委托人未要求本所代理，本所律师担任对方代理人 | 规范第 51 条第 4 项 |
| **委托关系终止后一年内，就同一法律事务接受与原委托人有利害关系的对方当事人委托** | 规范第 51 条第 5 项 |
| 其他近似情形 | 规范第 51 条第 6 项 |

### 👁 审慎评估（提示人工判断）

| 情形 | 依据（要点） |
|---|---|
| 委托终止一年以上，但新事务可能利用原委托中知悉的保密信息损害原委托人 | 《律师法》第 38 条保密义务 |
| 对方当事人的关联主体（近亲属、关联企业、法定代表人）出现在本所其他案件中 | 行业惯例与第 50 条第 8 项兜底 |
| 新旧案件案由、标的、事实高度相似但无法自动判定为同一法律事务 | 需律师人工确认 |

---

## 附录 D · 体验设计原则（供设计与实现参考）

1. **一览性**：打开看板就知道今天该干什么、有什么新动静——信息聚合优先于功能入口堆叠。
2. **案件即工作台**：办案所需的案情、资料、证据、时间轴、讨论、冲突报告、AI 集中在一个详情页，减少跳转。
3. **信任与严谨**：法律行业的 UI 要克制——信息密度优先、动效极少、关键操作（结案、删除、接案确认）二次确认、全程留痕。
4. **状态可感知**：期限、逾期、未读、冲突风险等级用「图标 + 文案 + 颜色」三重表达（不只用颜色区分）。
5. **零学习成本**：术语用律师熟悉的表述（如"举证期限""保全续封""利益冲突"），不发明新词。

---

*文档结束。确认后即可按 §12 交付 Trae 进入开发。*
