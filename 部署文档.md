# 案理（AnLi）· 律师案件协作平台 — 部署文档

## 技术栈

- **后端**：Node.js + Fastify + SQLite（node:sqlite 内置模块）
- **前端**：React 18 + Vite + Tailwind CSS
- **OCR**：macOS Vision 框架（Swift）
- **AI**：兼容 Ollama（本地）或 OpenAI 兼容接口

## 环境要求

- macOS（OCR 依赖 Vision 框架）
- Node.js >= 22（内置 `node:sqlite`）
- Swift 工具链（Xcode Command Line Tools）

## 本地开发

```bash
# 安装依赖
npm install

# 启动前后端开发服务器（端口 3000 后端 / 5173 前端）
npm run dev
```

默认管理员账号：`admin` / `admin123`

## 生产部署

### 1. 构建

```bash
npm run build
```

构建产物：
- `server/dist/` — 后端编译输出
- `client/dist/` — 前端静态文件

### 2. 启动

```bash
# 仅启动后端（后端会托管前端静态文件，需配置 static）
npm run start
```

### 3. 环境变量

| 变量 | 说明 | 默认值 |
|---|---|---|
| `PORT` | 后端端口 | 3000 |
| `JWT_SECRET` | JWT 密钥 | `anli-local-secret-change-me` |
| `OLLAMA_URL` | Ollama 服务地址 | `http://localhost:11434` |
| `OPENAI_BASE_URL` | 外部 AI 接口地址（可选） | - |
| `OPENAI_API_KEY` | 外部 AI 接口密钥 | - |
| `AI_MODEL` | AI 模型名称 | `qwen2.5:7b` |

### 4. 启用 AI 助手

```bash
# 方式一：本地 Ollama
ollama pull qwen2.5:7b
ollama serve

# 方式二：外部 OpenAI 兼容接口
export OPENAI_BASE_URL=https://api.openai.com/v1
export OPENAI_API_KEY=sk-xxx
export AI_MODEL=gpt-4o-mini
```

### 5. 数据备份

```bash
# 手动备份
./server/scripts/backup.sh

# 配置定时备份（每天凌晨 2 点）
crontab -e
0 2 * * * /path/to/AnLi/server/scripts/backup.sh
```

备份文件存储在 `data/backups/`，保留最近 30 天。

## 数据目录结构

```
data/
├── anli.db              # SQLite 数据库
├── cases/               # 案件资料文件夹
│   └── 2026-CIV-001-案件名称/
├── exports/             # 案件导出 zip
└── backups/             # 数据库备份
```

## 安全建议

1. **修改默认管理员密码** — 首次登录后立即修改
2. **设置 JWT_SECRET** — 生产环境必须更换
3. **配置 HTTPS** — 使用 Nginx 反向代理并启用 HTTPS
4. **限制文件上传大小** — 当前 500MB，按需调整
5. **定期备份** — 配置 crontab 自动备份数据库

## 案件类型

| 标识 | 名称 |
|---|---|
| civil | 民事 |
| civil_appeal | 民事二审 |
| civil_retrial | 民事再审 |
| criminal | 刑事 |
| administrative | 行政 |
| arbitration | 仲裁 |
| enforcement | 执行 |
| nonlitigation | 非诉专项 |
| legal_advisor | 常年法律顾问 |
| other | 其他 |
